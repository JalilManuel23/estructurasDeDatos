let numero=6;

if (numero % 2 == 0)
  {
      console.log(`El numero  ${numero} es par`);
      console.log(`El numero  ${numero} es par`);
  }
      
else
  {
    console.log(`El numero  ${numero} es impar`); 
    console.log(`El numero  ${numero} es impar`); 
  }


  //con operador ternario if sin if
  let numero=6;
  (numero % 2 == 0)?
    (
        console.log(`El numero  ${numero} es par`),
        console.log(`El numero  ${numero} es par`)

    )
    :
    (
        console.log(`El numero  ${numero} es impar`),
        console.log(`El numero  ${numero} es impar`) 
    )